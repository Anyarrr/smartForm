import { Grid, Typography } from "@mui/material"

export const Gratitude = () => {
    return(
        <Grid sx={{display: 'flex', justifyContent: 'center', alignItems: "center"}}>
            <Typography sx={{textAlign: 'center'}}>Благодарим за покупку!</Typography>
        </Grid>
    )
}