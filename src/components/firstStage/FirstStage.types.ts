export interface FirstStageProps {
    loading: boolean
    setLoading: (loading: boolean) => void
};

export interface ThridStageProps {
    thridLoading: boolean
    setThridLoading: (thridLoading: boolean) => void
}
